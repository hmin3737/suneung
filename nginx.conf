server {
    listen 80;
    server_name suneung.nemento.men;

    # HTTPS로 리다이렉트 (Let's Encrypt 설정 후 활성화)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }
}

# HTTPS (Let's Encrypt certbot 설정 후 추가)
# server {
#     listen 443 ssl;
#     server_name suneung.nemento.men;
#     ssl_certificate /etc/letsencrypt/live/suneung.nemento.men/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/suneung.nemento.men/privkey.pem;
#     ...
# }
